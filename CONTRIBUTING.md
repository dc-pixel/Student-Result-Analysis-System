# Contributing

- Keep SQL queries readable and parameterized where applicable.
- Validate result calculations with representative datasets before submitting changes.
- Run the repository validation workflow before opening a pull request.
- Document changes that affect schema, calculations, or user-facing output.

Use focused commit messages such as `feat: add grade summary`, `fix: handle missing marks`, or `test: cover result calculation`.
